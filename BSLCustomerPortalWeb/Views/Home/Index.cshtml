
@{
    ViewBag.Title = "Home Page";
    Layout = null;
}

@*<h2>LogIn</h2>*@

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
<link href="~/Content/bootstrap.css" rel="stylesheet" />
<script src="~/Scripts/jquery-3.4.1.min.js"></script>
<script src="~/Scripts/jquery-3.4.1.js"></script>
<script src="~/JS/sweetalert.min.js"></script>
@*<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>*@
@*<script src="https://unpkg.com/flickity@2/dist/flickity.pkgd.min.js"></script>*@


<script type="text/javascript">

    $(document).ready(function () {

    });

    function Fn_LogIn_Customer() {

        if ($("#txtCustomerId").val() == "") {
            swal("Login", "Please Enter Customer ID", "error");
        }
        else if ($("#txtPassword").val() == "") {
            swal("Login", "Password Field should not be Empty", "error");
        }
        else {

            var clsCustomer = {};
            clsCustomer.CustomerId = $("#txtCustomerId").val().trim();
            clsCustomer.CustPassword = $("#txtPassword").val().trim();

            $.ajax({
                type: "POST",
                url: "/Home/Fn_LogIn_Customer",
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                data: JSON.stringify(clsCustomer),
                success: function (data) {
                    var obj = JSON.parse(data.message);
                    if (obj.ErrorMsg == "Success") {
                        sessionStorage.setItem("CustId", obj.CustomerId);
                        sessionStorage.setItem("EmailId", obj.CustEmailId);
                        sessionStorage.setItem("CompanyName", obj.CustCompanyName);
                        sessionStorage.setItem("Mobile", obj.CustMobile);
                        sessionStorage.setItem("CustName", obj.CustName);
                        sessionStorage.setItem("CreateDate", obj.CustCreatedDate);
                        window.location.href = "@Url.Action("DashBoard", "Home")";
                    }
                    else {
                        swal("Login", obj.ErrorMsg, "error");
                    }
                },
                error: function (err) {
                    alert(err);
                }
            });
        }
    }


    function Fn_ShowPassword() {
        var passwordField = $("#txtPassword");
        var passwordFieldtype = passwordField.attr('type');

        if (passwordFieldtype === 'password') {
            passwordField.attr('type', 'text');
            $(this).find(".fa-eye-slash").hide();
            $(this).find(".fa-eye").show();
        }
        else {
            passwordField.attr('type', 'password');
            $(this).find(".fa-eye-slash").show();
            $(this).find(".fa-eye").hide();
        }
    }

</script>

<section>
    <div class="container" style="margin-top: 8%;">
        <div class="row justify-content-center">
            <div class="col-lg-5 col-md-6">
                <div class="card mb-0 h-auto" style="padding: 10px; border-radius: 12px; background: #fff; transition: box-shadow 0.3s ease; box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);">
                    <div class="card-body">
                        <div class="text-center mb-1">
                            <img src="~/Images/SingleLogoHighnoBG.png" width="35%" />
                        </div><br />
                        <h3 class="text-center mb-3 mt-0" style="color:#333; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; font-weight: 600;">Customer Login</h3>
                        <div>
                            <div class="form-group mb-3">
                                <label class="form-label" for="username" style="font-weight: 500; font-size: 13px;">Customer ID</label>
                                <div class="input-group">
                                    @*<span class="input-group-text"><i class="fa fa-user"></i></span>*@
                                    <input type="text" id="txtCustomerId" name="txtCustomerId" class="form-control" placeholder="Enter Your Customer ID" style="border-radius: 8px;">
                                </div>
                            </div>
                            <div class="mb-4 position-relative">
                                <label class="form-label" for="dlabPassword" style="font-weight: 500; font-size: 13px;">Password</label>
                                <div class="input-group">
                                    @*<span class="input-group-text"><i class="fa fa-lock"></i></span>*@
                                    <input type="password" id="txtPassword" name="txtPassword" class="form-control" value="" placeholder="Enter Your Password" style="border-radius: 8px;">
                                </div>

                                @*<span class="show-pass eye" onclick="Fn_ShowPassword();" style="position: absolute; top: 50%; right: 10px; transform: translateY(-50%); cursor: pointer;">
                                    <i class="fa fa-eye-slash"></i>
                                    <i class="fa fa-eye" style="display: none;"></i>
                                </span>*@
                            </div>
                            @*<div class="form-row d-flex flex-wrap justify-content-between mt-4 mb-2">
                                <div class="form-group ms-2" style="display:none;">
                                    <a class="btn-link" href="@Url.Action("", "")">Forgot Password?</a>
                                </div>
                            </div>*@
                            <div class="text-center">
                                <button type="submit" class="btn btn-warning btn-block" onclick="Fn_LogIn_Customer();" style="background: linear-gradient(90deg, #ffc600, #ff8c00); color: black; border-radius: 25px; font-weight: bold; font-size: 1.4rem; padding: 10px 15px; transition: background 0.3s ease-in-out;">
                                    Log In
                                </button>
                            </div>
                            <div class="new-account mt-3">
                                <p style="display:none;">New Customer <a class="text-primary" href="">Sign up</a></p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>